__pycache__/
*.egg-info/
.python-version
.DS_Store
.env
.venv
.ruff_cache/
.mypy_cache/
.pytest_cache/
dist/
build/
docs/_build/
.claude/settings.local.json

#projects/
# Large remote-pulled obs table (92.8GB source h5ad; reproducible via h5ad_obs/pull_obs.py).
# Full obs is 151MB (>GitHub limit); commit the compact obs.categoricals.parquet instead.
projects/**/h5ad_obs/obs.parquet

# Office lock/temp files and session scratch
~$*
*.tmp

# Non-shareable source PDFs and any local snippet index derived from them
# (index chunks/snippets contain verbatim paper text — keep out of the repo).
projects/**/inputs/do_not_share/
projects/**/local_index/
